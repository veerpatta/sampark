import type { NewTeacher } from "../schema";

/**
 * Class teachers.
 *
 * PLACEHOLDER — replace with the real VPPS staff list before running the seed.
 * `classes` uses whatever class-label convention you settle on in open
 * decision #3 ('12 Sci' vs '12-A Science' vs stream as a separate column).
 * Whatever you pick must match `students.class_label` exactly.
 *
 * Phone numbers are 10 digits, no country code — the WhatsApp link builder
 * prefixes 91.
 */
export const TEACHERS: NewTeacher[] = [
  // { id: "T01", name: "…", phone: "9XXXXXXXXX", classes: ["6"] },
];
