import { PhaseStub } from "@/components/admin/PhaseStub";

export default function DashboardPage() {
  return (
    <PhaseStub title="Dashboard" phase="Phase 2 · not built yet">
      Open requests, overdue requests, and the pending-review count. The status
      board is the enforcement mechanism — &ldquo;8 of 11 classes submitted&rdquo;
      shared in the staff group is what makes this stick.
    </PhaseStub>
  );
}
